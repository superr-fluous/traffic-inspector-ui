# Widget module

Responsible for rendering of widgets, enables users to manage widgets. Every widget is a closed box, that handles rendering and API-communication internally

# Input data

- widget ID

# Output data (callbacks)

**None**
